var config = {
    levelControllerConfig: {
       index: "test",
       typeName: 'data_elevator',
       connectionOptions: {
           host: "localhost:9200"
       }
    }
}

module.exports = config;